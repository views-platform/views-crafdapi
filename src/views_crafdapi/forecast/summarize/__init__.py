"""Summarize: reduce posterior samples to point + interval estimates via views-frames.

One reason to change — the statistical methodology that produces published MAP/HDI. Depends
only on the frozen views-frames-summarize leaf; carries no geography (CRP).
"""
